# Copy to .env and adjust values as needed

# Recommended: use a backend proxy to avoid CORS and keep secrets server-side
# Example local proxy URL:
REACT_APP_NEWS_API_BASE=http://localhost:3010/api/news

# Optional (DIRECT MODE ONLY): If calling NewsAPI directly from the browser (may cause CORS),
# provide your API key. Do NOT set this when using the proxy.
# REACT_APP_NEWS_API_KEY=your_newsapi_key_here
