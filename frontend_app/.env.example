# Example environment configuration for frontend_app
# Copy this file to .env and fill in the values as needed.

# Base URL for API calls (e.g., https://newsapi.org/v2 for direct mode or your proxy URL)
REACT_APP_API_BASE=<your_api_base_url>

# Optional: Direct NewsAPI key (only if calling NewsAPI directly from browser; prefer proxy to avoid CORS and exposing keys)
REACT_APP_NEWS_API_KEY=<your_newsapi_key_optional>
