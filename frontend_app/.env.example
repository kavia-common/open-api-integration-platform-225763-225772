# Frontend environment variables for React (Create React App)
# Only variables prefixed with REACT_APP_ are exposed to the browser.

# Preferred single base variable for news API access (STANDARDIZED)
# - Proxy mode (recommended to avoid CORS and keep API key secret):
#     REACT_APP_NEWS_API_BASE=http://localhost:3010/api/news
#     The proxy should expose:
#       GET /top-headlines
#       GET /search    (mapped to NewsAPI's /everything on the server)
#   DO NOT set REACT_APP_NEWS_API_KEY in proxy mode.
#
# - Direct mode (not recommended because of potential CORS and exposing your key):
#     REACT_APP_NEWS_API_BASE=https://newsapi.org/v2
#     REACT_APP_NEWS_API_KEY=YOUR_NEWS_API_KEY
#
# If REACT_APP_NEWS_API_BASE is not set, the app defaults to https://newsapi.org/v2 (direct mode).
REACT_APP_NEWS_API_BASE=

# Only required in direct mode. Leave empty or remove when using a proxy.
REACT_APP_NEWS_API_KEY=

# Deprecated/misnamed variables (do not use; kept here for awareness):
# REACT_APP_API_BASE
# REACT_APP_NEWS_APP_BASE
# REACT_APP_REACT_APP_NEWSAPI_KEY
# REACT_APP_REACT_APP_NEWS_API_BASE
